---
#---------- nginx ------------
- name: Install nginx using custom role
  hosts: myservers
  become: true

  roles:
    - nginx

#---------- my sql ------------
- name: Install mysql using galaxy
  hosts: db
  become: true

  vars:
    mysql_root_password: "StrongPassword123"
    
  roles:
    - geerlingguy.mysql
